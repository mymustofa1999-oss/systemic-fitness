          {/* Classification */}
          <tr>
            <td className={cn(lbl, 'w-24 align-top')}>Client Classification</td>
            <td colSpan={3} className={val}>
              {isEditingClass && canEditClass ? (
                <div className='flex items-center gap-2'>
                  <select
                    value={selectedClass}
                    onChange={(e) => setSelectedClass(e.target.value)}
                    className='flex-1 border border-slate-200 rounded px-2 py-1 text-sm focus:outline-none focus:ring-1 focus:ring-sf-warmGold/40'
                  >
                    <option value=''>- Unclassified -</option>
                    <option value='personal'>Personal</option>
                    <option value='group'>Group</option>
                    <option value='online'>Online</option>
                  </select>
                  <button type='button' onClick={handleSaveClass} disabled={updatingUser}
                    className='inline-flex items-center gap-1.5 px-3 py-1 rounded bg-sf-deepNavy text-white text-xs font-medium hover:bg-sf-deepNavy/90 disabled:opacity-50'>
                    {updatingUser ? <Loader2 className='h-3.5 w-3.5 animate-spin' /> : <Save className='h-3.5 w-3.5' />}
                    Save
                  </button>
                  <button type='button' onClick={() => setIsEditingClass(false)}
                    className='p-1 rounded hover:bg-slate-100 text-slate-400' title='Cancel'>
                    <X className='h-3.5 w-3.5' />
                  </button>
                </div>
              ) : (
                <button
                  type='button'
                  disabled={!canEditClass}
                  onClick={() => setIsEditingClass(true)}
                  className={cn(
                    'text-sm font-medium rounded px-1 py-0.5 -mx-1 w-full text-left',
                    canEditClass ? 'hover:bg-slate-50 transition-colors' : 'cursor-default'
                  )}
                >
                  {profile?.classification ? (
                    <span className='inline-flex items-center bg-sf-iceBlue text-sf-deepNavy border border-sf-iceBlue rounded-full px-2.5 py-0.5 text-xs font-medium capitalize'>
                      {profile.classification}
                    </span>
                  ) : (
                    <span className='text-slate-400'>- Click to classify -</span>
                  )}
                </button>
              )}
            </td>
          </tr>
