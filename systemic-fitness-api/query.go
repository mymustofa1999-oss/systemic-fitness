package main; import ("context"; "fmt"; "github.com/jackc/pgx/v5"); func main() { conn, _ := pgx.Connect(context.Background(), "postgres://fitcoach:fitcoach@localhost:5432/fitcoach?sslmode=disable"); rows, _ := conn.Query(context.Background(), "SELECT id, code, name FROM program_categories"); defer rows.Close(); for rows.Next() { var id, code, name string; rows.Scan(&id, &code, &name); fmt.Println(id, code, name); } }
